---
description: "Show gmax index status for all projects"
allowed-tools: "Bash"
---

Run `Bash(gmax status)` and show the user the results. If any projects show issues (not watching, stale index), mention it.
