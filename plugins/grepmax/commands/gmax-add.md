---
description: "Add current directory to gmax index"
allowed-tools: "Bash"
argument-hint: "[directory]"
---

Run `Bash(gmax add)` to add the current directory (or the specified directory) to the gmax index. Report how many files were indexed and confirm the watcher started.
