---
description: "Force reindex the current project"
allowed-tools: "Bash"
---

Run `Bash(gmax index)` to force a full reindex of the current project. Report how many files were reindexed when complete.
